import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

const baseState = {};

export default new Vuex.Store({
    state: baseState,
    getters: {},
    mutations: {},
    actions: {},
    modules: {}
});
